{
  "name": "fork-u-house-card",
  "private": true,
  "version": "0.0.0",
  "description": "Dev tooling for Fork U-House Card (tests only — the card itself is a single-file distribution)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
